# API Configuration Template - Auto IP Detection
# Copy this file to .env and run 'npm run auto-ip' to auto-detect IP

# Cho development trên máy local
API_HOST_LOCAL=localhost

# Cho iOS Simulator
API_HOST_IOS_SIMULATOR=127.0.0.1

# Cho Android Emulator
API_HOST_ANDROID_EMULATOR=10.0.2.2

# Cho thiết bị thật (Real Device) - Tự động detect bằng 'npm run auto-ip'
# Hoặc tìm thủ công: ifconfig | grep "inet " (macOS/Linux) hoặc ipconfig (Windows)
API_HOST_REAL_DEVICE=192.168.1.100

# API Port
API_PORT=3001

# Environment
NODE_ENV=development

# Debug
DEBUG_API=true