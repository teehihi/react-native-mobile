export interface PersonalInfo {
  name: string;
  age: number;
  occupation: string;
  school: string;
  major: string;
}

export interface Skill {
  name: string;
  level: number;
  color: string;
}

export interface SectionData {
  icon: string;
  title: string;
  items: string[];
  color: string;
}